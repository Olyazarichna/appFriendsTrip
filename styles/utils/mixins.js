
const fonts = (
    fontSize = 12,
    fontWeight = "500",
    lineHeight) => {
    
    return {
        fontSize: fontSize,
        fontWeight: fontWeight,
        lineHeight: lineHeight
}
}

export default fonts;